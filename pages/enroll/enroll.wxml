<view class="background">
    <view class="first">
        <input id='account' placeholder="用户名" placeholder-class="plas" type="text" class="inputs" model:value="{{account}}" />
    </view>
    <view class="second">
        <input id='account' placeholder="邮箱" placeholder-class="plas" type="text" class="inputs" model:value="{{email}}" />
        <button class="button" bindtap="onGetCode">{{trip}}</button>
    </view>
    <view class="second">
        <input id='pwd' placeholder="验证码" placeholder-class="plas" type="password" class="inputs" model:value="{{code}}" />
    </view>
    <view class="second">
        <input id='pwd' placeholder="登录密码" placeholder-class="plas" type="password" class="inputs" model:value="{{password}}" />
    </view>
    <view id='btn' class="click" bindtap='onEnroll'>注册</view>
</view>